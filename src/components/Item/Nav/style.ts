import styled from "styled-components";

export const ItemWrap = styled.div`
  color: #fff;
  font-weight: 600;
  gap: 6px;
  display: flex;
  align-items: center;
  text-decoration: none;

  &:hover {
    color: #ccc;
    fill: currentColor;
  }
`;
